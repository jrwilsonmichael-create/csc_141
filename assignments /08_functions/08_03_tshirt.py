# I will write a function of tshirts to the cart
def add_tshirt_to_chart(size, color, quantity=1):
    """Add a tshirt to the chart."""
    print(f"Added {quantity} {color} tshirt(s) of size {size} to the chart.")